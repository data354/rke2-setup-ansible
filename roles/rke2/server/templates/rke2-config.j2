{% if master | bool == false %}
server: https://{{ groups['control_plane'][0] }}:{{ join_cluster_port }}
{% endif %}
token: {{ cluster_token }}
tls-san:
{% for item in loadbalancer_addresses -%}
    - {{ item }}
{% endfor -%}